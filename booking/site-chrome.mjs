/**
 * The site header and footer around the booking pages: none. The pages are complete
 * without them. To wear your own site's chrome, return its header and footer HTML here
 * (and style it in pages.mjs APP_CSS).
 */
export function siteHeader() { return ""; }
export function siteFooter() { return ""; }
